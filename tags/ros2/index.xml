<?xml version="1.0" encoding="utf-8" standalone="yes"?><rss version="2.0" xmlns:atom="http://www.w3.org/2005/Atom"><channel><title>ros2 on HSMINT's BLOG</title><link>https://blog.hsmint.dev/tags/ros2/</link><description>Recent content in ros2 on HSMINT's BLOG</description><generator>Hugo -- gohugo.io</generator><language>en-us</language><lastBuildDate>Thu, 13 Aug 2026 09:00:00 +0900</lastBuildDate><atom:link href="https://blog.hsmint.dev/tags/ros2/index.xml" rel="self" type="application/rss+xml"/><item><title>TIL 2026-08-13 | ROS2 Lifecycle · Composition · Launch와 colcon 실행 경로 문제</title><link>https://blog.hsmint.dev/blog/ros2-lifecycle-composition-colcon/</link><pubDate>Thu, 13 Aug 2026 09:00:00 +0900</pubDate><guid>https://blog.hsmint.dev/blog/ros2-lifecycle-composition-colcon/</guid><description>오늘의 문제 (Why · How · What) Why — 무엇이 문제였나 colcon build 중 CMakeLists.txt에서 executable을 링크하는 과정에서 오류가 발생했다. 빌드 자체는 통과하는 경우도 있었지만, 정작 실행 시점에 필요한 라이브러리/패키지 경로를 찾지 못해 실행이 되지 않는 문제였다.
즉 증상은 두 갈래였다.
링크 단계에서 곧바로 실패하는 경우 빌드는 성공했는데 실행하면 의존성을 찾지 못하는 경우 How — 어떻게 해결했나 실행파일이 의존성을 찾아가는 경로 설정을 손봐서 해결했다. 구체적으로는 아래 세 환경변수가 실행 시점의 탐색 경로로 쓰인다는 것을 확인하고, 값이 올바르게 잡혀 있는지 점검·수정했다.</description></item><item><title>[TIL] ROS2 통신 총정리 — Executor·Topic/Service/Action·커스텀 인터페이스·QoS·TF2</title><link>https://blog.hsmint.dev/blog/til-ros2-communication/</link><pubDate>Wed, 12 Aug 2026 21:00:00 +0900</pubDate><guid>https://blog.hsmint.dev/blog/til-ros2-communication/</guid><description>1. 노드 실행 모델 — 콜백, Executor와 spin ROS2 시스템은 **노드(node)**들이 **토픽(topic)**으로 연결된 계산 그래프다. 노드는 하나의 일을 하는 독립 프로세스, 토픽은 이름 붙은 단방향 메시지 흐름이다. 발행자는 구독자가 누군지 모르고(느슨한 결합), 노드는 독립 프로세스라 하나가 죽어도 나머지는 계속 돈다.
노드의 동작은 전부 콜백(구독 콜백·타이머 콜백·서비스 콜백)으로 등록된다. 순차 실행 스크립트가 아니라 &amp;ldquo;이 이벤트가 오면 이 함수를 불러라&amp;quot;를 등록해두는 이벤트 구동 모델이다.
class PerceptionNode(Node): def __init__(self): super().__init__(&amp;#39;perception_node&amp;#39;) self.sub = self.create_subscription(LaserScan, &amp;#39;/scan&amp;#39;, self.</description></item><item><title>[TIL] 스마트 포인터 leaks 실측 · ROS2 Executor/spin · TurtleBot3 Navigation</title><link>https://blog.hsmint.dev/blog/til-cpp-ros2/</link><pubDate>Tue, 11 Aug 2026 21:00:00 +0900</pubDate><guid>https://blog.hsmint.dev/blog/til-cpp-ros2/</guid><description>1. macOS leaks로 실측하는 스마트 포인터의 메모리 누수 방지 실험 코드 — 매크로 하나로 raw/smart 스위칭 #ifndef USE_SMART_POINTER #define USE_SMART_POINTER 0 #endif class Motor { int id_; double speed_ = 0.0; public: explicit Motor(int id) : id_(id) {} void setSpeed(double v) { speed_ = v; } }; int main() { #if USE_SMART_POINTER for (int i = 0; i &amp;lt; 1000; ++i) { auto m = std::make_unique&amp;lt;Motor&amp;gt;(i); m-&amp;gt;setSpeed(i); } #else for (int i = 0; i &amp;lt; 1000; ++i) { Motor* m = new Motor(i); m-&amp;gt;setSpeed(i); } #endif } Motor를 1000번 만들고 버리는 루프를 raw new와 make_unique 두 가지 방식으로 각각 실행 #ifndef USE_SMART_POINTER 덕분에 컴파일 시 -D 플래그 하나로 두 버전을 같은 소스에서 뽑아낼 수 있음 컴파일 — 매크로 값 주입 g++ -Wall -Wextra -std=c++17 -DUSE_SMART_POINTER=0 -o leaks_raw leaks_demo.</description></item></channel></rss>